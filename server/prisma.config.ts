// This file is kept for legacy compatibility but is no longer used.
// Prisma is now configured via the "prisma" key in package.json.
// Schema path: src/prisma/contract.prisma
// Run: bun run db:generate   (prisma generate)
//      bun run db:push        (prisma db push -- dev only)
//      bun run db:migrate     (prisma migrate dev -- production)
//      bun run db:seed        (seed Plans table)
export {};
